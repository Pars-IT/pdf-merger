[project]
name = "pdf-merger"
version = "0.1.0"
description = ""
authors = [
    {name = "Mohammad",email = "habibi.mh@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pypdf (>=6.10.2,<7.0.0)"
]

[tool.poetry]
packages = [{include = "pdf_merger", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
